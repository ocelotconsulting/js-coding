describe('most used character', () => {
  // return the character that is used the most in the input string, excluding spaces
  // in case of a tie (multiple characters used the most) return the first one that appears in the string
  function mostUsedCharacter(s) {
  }

  it('tests', () => {
    expect(mostUsedCharacter('once upon a time')).toBe('o')
    expect(mostUsedCharacter('ice cream pie')).toBe('e')
  })

})
